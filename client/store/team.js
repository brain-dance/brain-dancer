import axios from 'axios';

// ACTION CONSTANTS

// ACTION CREATORS

// THUNKS

// INITIAL STATE

// REDUCER
const reducer = (state, actions) => {
  switch (action.type) {
    default:
      return state;
  }
};

// EXPORT
export default reducer;
